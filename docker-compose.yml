services:
  jamarr_db:
    image: postgres:18.1-alpine
    container_name: jamarr_db
    network_mode: "host"
    command: postgres -p 8110
    restart: unless-stopped
    environment:
      POSTGRES_USER: jamarr
      POSTGRES_PASSWORD: jamarr
      POSTGRES_DB: jamarr
    volumes:
      - /mnt/config/q-docker/jamarr/postgres:/var/lib/postgresql/data


  jamarr:
    build: .
    container_name: jamarr
    network_mode: "host"
    depends_on:
      - jamarr_db
    volumes:
      - type: volume
        source: music
        target: /app/music
        read_only: true
        volume:
          nocopy: true
      - /mnt/config/q-docker/jamarr:/app/cache
      - ./config.yaml:/app/config.yaml
    restart: unless-stopped
    environment:
      - HOST=0.0.0.0
      - HOST_IP=${HOST_IP:-127.0.0.1}
      - HOST_PORT=8111
      # Database connection info (Hardcoded for now as requested)
      - DB_HOST=127.0.0.1
      - DB_PORT=8110
      - DB_USER=jamarr
      - DB_PASS=jamarr
      - DB_NAME=jamarr


volumes:
  music:
    driver_opts:
      type: "nfs"
      o: "addr=192.168.1.201,nolock,soft,rw"
      device: ":/mnt/tank/music/library"
